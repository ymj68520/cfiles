#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

class Solution {
public:
    bool check(vector<int>& nums) {
        int n = nums.size();
        if(n == 1)  return true;
        int start = nums[0];
        int mid = 0;
        for(int i=1;i<n;i++){
            if(nums[i] < nums[i-1]){
                if(nums[i<])
            }
        }
        return true;
    }
};